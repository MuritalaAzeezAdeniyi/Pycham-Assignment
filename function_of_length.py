def length_of_collection(list):
    sum = 0
    for word in (list):
        sum += 1
        print(length_of_collection ("wood"))

       def length (items):
        count = 0
        if type(item) in [int,float]
            raise valu  ("argument must be iterable")
        for item in items:
            count += 1
            return count

        from playground import length
        assert length([1, ade]) == 2
        assert length([]) == 0














